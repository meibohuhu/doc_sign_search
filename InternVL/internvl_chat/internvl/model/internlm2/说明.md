Layer 0:
  hidden_states₀ → attention_norm → wqkv → qkv_states₀ → flash_attention → hidden_states₀' (更新)
  
Layer 1:
  hidden_states₁ = hidden_states₀' → attention_norm → wqkv → qkv_states₁ → flash_attention → hidden_states₁' (更新)
  
Layer 2:
  hidden_states₂ = hidden_states₁' → attention_norm → wqkv → qkv_states₂ → flash_attention → hidden_states₂' (更新)


hidden_states 会被更新：每层经过 attention 和 FFN 后更新
hidden_states 是上一层的输出：作为当前层的输入
每一层的 qkv_states 都是新的：因为 qkv_states = wqkv(hidden_states)，而每层的 hidden_states 不同




Currently:
token-conditioned、text-driven 的 head-level / element-level value gate
关键事实（从你代码结构出发）：
gate_score_raw 来自 wqkv(hidden_states)
hidden_states = 当前 decoder layer 的 token 表示
在 Qwen / InternLM VLM 里，这个 token 可能是：
  text prefix token
  image/video token（patch / frame / pooled）

或二者混合后的 hidden state
  gate 的 conditioning 是局部的、token-wise 的
  每个 token 都有 gate
  gate 是 σ(gate_score_raw)
  再 逐 head（或逐 dim）乘在 attn_output 上

🔴 这意味着：当前 gate ≈「由 token 自身内容决定，该 token 的注意力输出该放大/抑制多少」

1️⃣ 定义一个 visual summary（概念上）
其中 Agg 可以是：
mean pooling
attention pooling
CLS / first visual token
你甚至可以说“由 VLM encoder 给出的视觉全局表示
注意：这和你现在的 gate 唯一的概念差异是
现在：g 由 token hidden state 决定
Task-1：g 由 visual global state 决定





Qwen2 gate 
=== Qwen2 Attention Structure ===
Has q_proj: True
Has k_proj: True
Has v_proj: True
Has o_proj: True
q_proj shape: torch.Size([896, 896])
k_proj shape: torch.Size([128, 896])
v_proj shape: torch.Size([128, 896])
o_proj shape: torch.Size([896, 896])
Attention class: Qwen2SdpaAttention
Module: transformers.models.qwen2.modeling_qwen2

Config attributes:
  num_attention_heads: 14
  num_key_value_heads: 2
  hidden_size: 896
  head_dim: 64

attention score 的形状：
  attention_score ∈ R^{B × H × S × S}

也就是说：

每个 batch
每个 head
每个 query token
→ 一个 scalar gate


visual-conditioned gate（proposal）
| 维度      | Qwen3 / 你现在      | visual-conditioned      |
| ------- | ---------------- | ----------------------- |
| gate 条件 | hidden_states[i] | **visual context**      |
| gate 类型 | self-gating      | **cross-modal gating**  |
| 作用对象    | output magnitude | **routing bias**        |
| 是否模态感知  | ❌                | ✔️                      |
| 跨样本差异   | 小                | **同一句话，不同视频 → 不同 gate** |

While the Qwen3-style gated attention derives gating signals directly from token hidden states to modulate attention outputs, our proposed visual-token-conditioned gating explicitly aggregates visual tokens to form a cross-modal conditioning signal, which is injected into the attention scores to enable modality-aware attention routing.


你是在对 gate 这个变量，额外加一个 loss
低秩路由：
Layer ℓ:
[0.02, 0.01, 0.97, 0.03, 0.95, 0.00, ...]

你现在模型里有很多 attention head：Head 1, Head 2, Head 3, ..., Head 32
但 ASL 翻译真正有用的 head 可能只有 3–5 个。
所谓“低秩路由”，直白说就是：
大多数 head 被关掉，只有少数 head 在真正工作

如果你什么都不做，gate 会怎样？
如果你不给 gate 任何约束：
所有 gate 都可能学成 0.5～0.8
所有 head 都“有点参与”
噪声 head 也会吃梯度
你得不到“少数关键 head”


是的，gate 的学习目标应该是让 gate 值分散，以体现选择性。
如果训练后所有 gate 值都集中在中间，可能说明：
Gate 没有学到有效的选择性（所有位置都被同等缩放）
初始化或学习率设置导致 gate 权重被过度优化
损失函数没有鼓励 gate 学习选择性
